name: 生成
on: [ push, pull_request ]

jobs:
    static-checks:
        name: 📊 静态检查
        uses: ./.github/workflows/static_check.yml
    
    cmake_matrix:
        name: CMake 矩阵编译
        needs: static-checks
        uses: ./.github/workflows/cmake-matrix.yml